spec bad_push {
    input:
        k: i64
    output: i64
    ensures:
        output >= 0
}
fn bad_push(k: i64) -> i64 {
    let v = vec_new()
    vec_push(v, k)
    return vec_get(v, 0)
}
fn main() { print(bad_push(0 - 5)) }
