// M5: рекурсия с assume-guarantee (частична коректност).
// Индукционната хипотеза носи ensures на рекурсивното повикване:
//   r = sum_to(n-1), r >= 0  ⇒  n + r >= 0.
// (M7 integer tightening доказва requires на повикването от n > 0: n - 1 >= 0.)
spec sum_to {
    input:
        n: i64
    output: i64
    requires:
        n >= 0
    ensures:
        0 <= output
}

fn sum_to(n: i64) -> i64 {
    if n <= 0 { return 0 }
    let r = sum_to(n - 1)
    return n + r
}

fn main() {
    print(sum_to(10))
}
