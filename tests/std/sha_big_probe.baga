import "../../std/crypto/sha256.baga"
fn main() {
    let mut s = "aaaaaaaaaa"
    let mut i = 0
    while i < 17 {
        s = concat(s, s)
        i = i + 1
    }
    print(len(s))
    print(sha256_hex(s))
}
