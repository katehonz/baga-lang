import "std/time/time.baga"

fn check(name: str, ok: bool) {
    if ok {
        print(concat("ok ", name))
    } else {
        eprintln(concat("FAIL ", name))
        exit(1)
    }
}

fn main() -> i64 !Time {
    let now = time_now_ms()?
    check("now_positive", now > 1700000000000)   // after 2023-11-14
    let m1 = monotonic_ms()?
    let mut spin: i64 = 0
    for i in 0..1000000 {
        spin = spin + i
    }
    let m2 = monotonic_ms()?
    check("monotonic_advances", m2 >= m1)
    check("spin", spin >= 0)
    let u1 = monotonic_us()?
    spin = 0
    for i in 0..100000 {
        spin = spin + i
    }
    let u2 = monotonic_us()?
    check("monotonic_us_advances", u2 >= u1)
    check("monotonic_us_scale", u1 > 0)
    print("time_test: all passed")
    return 0
}
