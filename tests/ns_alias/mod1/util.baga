// util.baga (mod1) — alias test module (L6): same basename as mod2/util.baga.
fn who() -> str {
    return "mod1"
}

fn one() -> i64 {
    return 1
}

// same struct name as mod2 — L6 struct qualification probe
struct Rec {
    v: i64
}

fn mk() -> Rec {
    return Rec { v: 1 }
}
