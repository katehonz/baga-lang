# reportbaga — gaps

## Closed 0.1.0

- HTML first `<table>` → CsvTable → XLSX / CSV
- `fixtures/oborotna.html` accounting-style demo
- CSV → XLSX bridge

## Open

### R1 — only first table

Multiple tables / multi-sheet Excel later.

### R2 — no PDF yet

`pdfbaga` next for print path.

### R3 — HTML must be well-formed XHTML

Uses xmlbaga; lenient HTML5 not supported.

### R4 — no number formatting / freeze panes

Amounts are strings as in HTML.
