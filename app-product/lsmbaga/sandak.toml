[package]
name = "lsmbaga"
version = "0.1.1"
entry = "shim_server.baga"
description = "Deprecated alias — use rocksbaga"

[dependencies]
rocksbaga = { path = "../rocksbaga" }
std = { path = "../../std" }
