# Latency microbench — 2026-08-05

**B4.4 gate.** Checked-in numbers from `./bench/run_latency.sh` for regression
reference (not a product SLA).

## Host

| Field | Value |
|-------|--------|
| Date | 2026-08-05T23:03:08+03:00 |
| Kernel | Linux 6.1.0-51-amd64 x86_64 |
| CPU | AMD Ryzen 5 3600 6-Core Processor |
| baga | 0.7.0 |

## Workload

`bench/latency.baga`: 40 batches × `work(8_000_000)` pure integer loop;
samples are batch wall times via `monotonic_ms`. p50/p99 over sorted batch
times.

## Results (run 2, recorded)

| Metric | ms |
|--------|-----|
| wall (all batches) | 281 |
| batch min | 6 |
| batch avg | 7 |
| batch max | 8 |
| **batch p50** | **7** |
| **batch p99** | **8** |
| work_checksum | 352047104 |

Run 1 (same session, moments earlier): p50=6 p99=7 wall=248 — small noise.

## How to re-run

```bash
./bench/run_latency.sh
# optional: BENCH_N=5000 ./bench/run_latency.sh  (reserved; batch count is fixed)
```

Copy stdout into a new `bench/results/latency-YYYY-MM-DD.md` if numbers move
more than ~2× without an intentional change.
