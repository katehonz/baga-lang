# ormbaga × boilaDB — live PG wire, 2026-08-13

След RC5 v1.0 (`b3b8a2b`). Продуктовият път: **ormbaga → boilabaga →
pgbaga → boilaDB `serve_pg`**.

## Сървър

```
./baga -I . -I app-product app-product/boilaDB/tools/serve_pg.baga
# BOILA_PATH=/tmp/boila_orm_live  BOILA_PGPORT=6575  BOILA_SHARDS=2
```

`boiladb-pg wire listening on :6575` (go_bg + per-shard + multi-db).

## `tests/orm_boila_test.baga` — 36/36

Клиент без `--rc` срещу сървър без `--rc`:

```
orm_boila_test: all passed
```

| група | проверки |
|-------|----------|
| dialect (`ident_ok`, `sql_ident_bare`, `ddl_mig_table`) | 3 |
| connect / ping | 2 |
| migrate down/up/idempotent/current | 5 |
| CRUD users (insert/where/find/update) | 7 |
| posts + count | 2 |
| quote (`O'Hara`) | 2 |
| `INSERT … RETURNING` + delete | 3 |
| migrate down_1 / re_up / full_down | 3 |
| `orm_from_conn` / auto | 3 |
| pool acquire/query/release | 6 |

Клиент **с `--rc`** срещу същия сървър (без `--rc`): **36/36**, същият
изход. `boila_ps_tok` (borrowed `Token` от `vec_get`) е на горещия parse
път — v1.0a retain-ва резултата; няма underflow, няма dangling.

## `--rc` serve_pg — 36/36

След фикс на dummy `BoilaPgOut`, `srv2 = ro.srv` след rewind, и
`map_set` overwrite на singleton сървър / same-gen plan cache:

```
BOILA_PGPORT=6584 ./baga --rc … tools/serve_pg.baga
BOILA_PGPORT=6584 ./baga [--rc] … tests/orm_boila_test.baga
→ orm_boila_test: all passed
```

Клиент с и без `--rc` срещу `--rc` сървър: **36/36**. Сървърът остава
жив след теста.

## `ormbaga/examples/demo_boila.baga`

```
connected server_version=16.0 (boilaDB 0.1.0)
migrate_up applied=3
status:
20260811001 create_users [applied]
20260811002 create_posts [applied]
20260811003 index_posts_user_id [applied]
insert user: INSERT 0 1
find user id=1 name=Bagatur
update: UPDATE 1
insert post: INSERT 0 1
posts for user: 1
users.count = 1
ping ok=1
done
```

## `tests/orm_test.baga` (Postgres, контрола)

`orm_test: all passed` — Postgres пътят не е регресирал.

## Къде сме

- boilaDB **P0–P11 са качени**. ORM слоят (ormbaga P2.2) работи end-to-end
  през PG wire срещу жив `serve_pg`.
- `orm_boila` в `baga-test` без peer остава FAIL — това е очакваният
  external-peer (няма `serve_pg` в discovery runner-а), не регресия.
- `--rc serve_pg` е жив (36/36). Следващо продуктово: filesize gate
  (P11-FS, 8 файла > 400 реда).
