# raftbaga (S7) Implementation Plan

**Goal:** 3-node in-process Raft: election + single-entry log replication + KV apply, over channels.

**Spec:** `docs/superpowers/specs/2026-08-05-raftbaga-design.md`

### Task 1: msg + pure rules
- [x] `msg.baga` packing/unpacking
- [x] `rules.baga` term/vote pure functions + specs for `--verify`

### Task 2: node loop
- [x] `node.baga` follower/candidate/leader, AE/RV, apply

### Task 3: cluster + client
- [x] `cluster.baga` start/stop/put/get

### Task 4: tests + docs
- [x] `tests/raft_test.baga`, demo, README, gaps, CHANGELOG, cloud plan
