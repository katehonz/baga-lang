# Self компилатор: enum (M3c) — План

> **For agentic workers:** Steps use checkbox (`- [ ]`) syntax. БЕЗ git мутации.

**Goal:** `cvet.baga` през baga2 — идентичен изход с baga (`1` и `зелено`).

**Spec:** `docs/superpowers/specs/2026-07-31-self-enum-design.md`

**Ключови файлове:** `self/compiler.baga` (top-level loop, parse_struct ~ред 422
(модел), emit_expr ident ~ред 665, emit struct decl ~ред 1290 (модел)). Целеви C
(codegen_c.c): decl `typedef enum { em_v = j } em;`, референция `em_v`.

## Global Constraints

- Адитивно. Оракул 16/16. `make test` зелен. `make self` (fixed point) зелен.
  mangle консистентно. Без git мутации.

---

### Task 1: Парсер — enum (28) + variant (29)

**Files:** Modify `self/compiler.baga`

- [ ] **Step 1: `parse_enum`** (нова fn, модел parse_struct)

advance "enum"; име; `{`; цикъл варианти (ident → node 29, `,` се прескача) до
`}`; node 28 (text=име) с деца 29.

- [ ] **Step 2: top-level loop**

До "struct": при ident "enum" → parse_enum, ast_child(prog, s).

### Task 2: Codegen — typedef + референция

**Files:** Modify `self/compiler.baga`

- [ ] **Step 1: emit enum typedef** (преди struct decl-ите)

За node 28: `typedef enum {\n` + за всяко дете 29 (индекс vi):
`    mangle(enum)_mangle(var) = vi,\n` + `} mangle(enum);\n\n`.

- [ ] **Step 2: `enum_variant_cname`** (нова fn)

scan prog за node 28 → дете 29 с име == name → `mangle(enum)_mangle(name)`;
иначе `""`.

- [ ] **Step 3: emit_expr ident (k==2)**

`let ev = enum_variant_cname(...)`; ако `ev != ""` → return ev; иначе mangle(t).

### Task 3: Проверка + регресия

- [ ] **Step 1:** baga2 се компилира; `/tmp/baga2 examples/cvet.baga` → C → gcc
  → `1` и `зелено` (идентично с baga).
- [ ] **Step 2:** `make self` → fixed point; `make test` зелен; baga vs baga2 —
  cvet минава в OK, 11-те OK без регресия.

---

## Self-Review бележки

- Coverage: парсер (T1), codegen (T2), регресия (T3).
- Тънки места: (1) вариант = `mangle(enum)_mangle(var)` (също като C);
  (2) enum_variant_cname сканира по име — cvet има уникални варианти;
  (3) `let c = Зелено` → ctype int64_t (enum константа е int) — коректно;
  (4) match в cvet ползва int лит. (0/1/2), не варианти — работи от M3b;
  (5) enum typedef ПРЕДИ struct/forward decls (C прави enums first).
