# lsmbaga MVP (S5+S6) Implementation Plan

> **For agentic workers:** implement task-by-task. No git commits by
> implementers. Each task ends with a green smoke of what it added.

**Goal:** Durable LSM-style KV on RESP using page cache + WAL + SSTable
flush + compaction-lite.

**Spec:** `docs/superpowers/specs/2026-08-05-lsmbaga-design.md`

**Tech:** pure Baga package `app-product/lsmbaga`, std/os extensions,
`tests/lsm_test.baga` + `tests/std/os_fs_test.baga`.

---

### Task 1: std/os filesystem + binary IO

**Files:** `std/os/os.baga`, `std/os/README.md`, `tests/std/os_fs_test.baga`

- [x] `extern mkdir` / `unlink` / `link` + `fs_rename` (link+unlink; raw rename collides with stdio.h)
- [x] `fd_write_bytes`, `fd_pwrite_bytes`, `fd_pread_bytes` (tcp binary idiom)
- [x] `tests/std/os_fs_test.baga`

### Task 2: page cache (S5)

**Files:** `app-product/lsmbaga/page.baga`

- [x] `pc_new` / `pc_get` / clock eviction / dirty writeback / invalidate (return-updated struct)

### Task 3: codec + WAL + SSTable

**Files:** `codec.baga`, `wal.baga`, `sstable.baga`

- [x] LE u32 helpers, WAL append/replay, SST write/scan, crc32c checks

### Task 4: engine

**Files:** `engine.baga`

- [x] open/close, put/get/del, flush, compact, recovery, KEYS/DBSIZE view

### Task 5: RESP server + package

**Files:** `server.baga`, `demo.baga`, `sandak.toml`, `README.md`, `gaps.md`, `PLAN.md`

- [x] Import `kvbaga/resp.baga`; serial serve; env `LSMPATH` / flush/compact

### Task 6: integration test + docs

**Files:** `tests/lsm_test.baga`, CHANGELOG, README, cloud plan status

- [x] Recovery + flush + compact + RESP checks
- [x] baga-test discovery (`tests/lsm_test.baga`)
