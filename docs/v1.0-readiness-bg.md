# v1.0 readiness — оценка за стабилизиране на езика

Дата: 2026-08-22. Статус: main след v0.9.2 (Unreleased секцията натрупва само
hardening — без нови езикови фичи от седмици).

## Снимка на здравето (проверено на тази дата)

- `scripts/run_tests.sh`: 192/192 зелени (orm_boila_test изисква ръчно пуснат
  `serve_pg` на `BOILA_PGPORT` — виж коментара в `tests/orm_boila_test.baga:4`).
- `scripts/run_verify.sh`: зелена, включително LP6 адверсариалната soundness
  батерия (10/10 фалшиви твърдения оброчени, без фалшиво ДОКАЗАНО).
- `scripts/self_parity.sh`: зелена — 45 PARITY, fixed-point самокомпилация,
  без поведенческа дивергенция спрямо bootstrap-а.

## Граници, финални по дизайн (кандидати за „замразяване" в v1.0)

Документирани честно; да не се пипат:

- MEM-1 договорът за алиасинг: checker-ът следи променливи, не heap графи;
  `let y = x; drop(x); use(y)` не се диагностицира (`docs/language-bg.md:1008`).
- Алиаси през контейнери (`vec_push(v2, x)`) са недоловими (MEM-1 договор).
- Няма GC / borrow checker / lifetimes / linear types; цикли текат — няма weak
  refs (двата бекенда) (`docs/memory-rc-bg.md:47-58,311`).
- Runtime free-list: блокове > 1024 B не се reclaim-ват (pow2 класове до
  32 MiB компенсират частично); исторически боклук от `vec_grow`/`map_rehash`
  не се следи (`docs/language-bg.md:1016-1018`).
- Immortal escape handle-и `map_h`/`str_h`/`bytes_h` никога не release-ват
  (leak-safe по дизайн) (`docs/memory-rc-bg.md:274-281`).
- LLVM изключения по дизайн: без масиви/референции; `mem_mark/rewind/persist`
  само C бекенда; `persist` не се поддържа; `map_h`/`h_map` C-only; page guard
  вместо arena range guard в `baga_rc_hdr` (`docs/language-bg.md:1032-1034`).
- Verify методологията: честен skip извън фрагмента, никога фалшиво ДОКАЗАНО;
  fn-стойности непрозрачни за верификатора; частична коректност без терминация;
  проза-гаранции извън фрагмента (`docs/language-bg.md:911-912,1307-1316`).
- Self-host = доказателство за коректност, не продукция: 5–10× по-бавен, без
  ефектен/trait checker (`docs/compiler-bg.md:528-534`).
- Дребни: enum имената са глобални (`src/checker.c:3838`); `vec_slice` clamp-ва
  вместо грешка; `str_of_bytes` отрязва до първия NUL; няма имплицитни
  i64↔f64 конверсии; f64 не е Map ключ.
- Споделени RC граници на двата бекенда (потвърдени в кода при
  прекласификацията на „LLVM изключенията"): closure capture-ите са borrowed
  и не се retain-ват; цикли текат (няма weak).

## Временни граници — решение преди v1.0 (затвори или roadmap-ни)

1. ~~**LLVM `--rc` изключения**~~ ✅ — паритетът е пълен:
   - ~~statement-ниво temp-ове не се release-ват~~ ✅ (RC4 е портнат:
     temp-овете се emit-ват преди root израза, кешират се по AST възел и се
     release-ват в края на statement-а; consume при move в контейнер);
   - ~~match scrutinee temp-ове не се release-ват~~ ✅ (v0.11: enum ctor с
     heap payload и owned fn резултат като scrutinee се release-ват след
     рамената, в края на statement-а);
   - closure captures не се retain-ват и (тогава) raise пътят не release-ва
     локали — **прекласифицирани**: проверката в кода показа, че C бекендът
     прави същото (captures са borrowed/`is_param=1` в wrapper-а,
     codegen_c.c:3542). Captures остават споделена финална граница; raise
     пътят след това е затворен и в двата бекенда (точка 7).
2. **Generics ръбове**: ~~generic fn като стойност (`src/checker.c:2356`)~~
   ✅ — с явна fn-тип анотация (M25, `tests/generic_fn_value_test.baga`;
   неанотираният binding остава грешка);
   ~~`--rc` helper-и за heap полета на generic struct (M24)~~ ✅ — per
   инстанция и в двата бекенда (`tests/generic_struct_rc_test.baga`);
   `Vec<Vec<T>>` с
   типова променлива (leak-safe); ламбди в generic тяло; spec върху generic.
3. ~~**Стари TODO-та**~~ ✅ — `src/proofs.c` `is_recursive` (мъртва
   евристика) е изтрит; `src/checker.c:2513` NODE_INDEX TODO-то е заменено с
   честен коментар (`x[i]` = суров C subscript примитив, елементен тип не се
   следи).
4. **std/net TLS сървър** — има само TLS 1.3 клиент; boilaDB отговаря `'N'`
   на SSLRequest.
5. **Self покритие извън PARITY_LIST**: C1 сигнали, drop/MEM-1, mem_*,
   `str_h`/`map_h` cast-ове, struct със sum-enum поле, generic fn от generic fn.
6. **Verify отворени проблеми** (не са v1 блокери, но да са видими в roadmap):
   channel liveness, пълен bitvector, богати полиноми
   (`docs/thesis-open-problems.md`).
7. ~~**Release на локали при изход през ефект (ДВАТА бекенда)**~~ ✅ —
   `raise`/`?`/catch-propagate пътищата release-ват RC локалите преди изхода;
   не-fresh heap payload се retain-ва в слота преди release-ите, fresh
   payload се move-ва. Gate: `tests/raise_rc_test.baga` в `tests/llvm_rc.sh`.

## ~~Риск за v1.0: тихи fallback-и в C бекенда~~ — затворен (2026-08-22)

Беше: `src/codegen_c.c` `emit_expr`/`emit_stmt` default клонове емитваха тихо
`0 /* unhandled expr %d */`. Сега: `c_unsupported_node` дава шумна
диагностика + exit 1 — същата политика като `llvm_unsupported` в LLVM
бекенда. Fallback-ите не бяха достижими от валидни програми (батерията
остана зелена без промяна).

## Препоръчани стъпки към v1.0

1. ~~Шумни диагностики вместо тихите fallback-и в `codegen_c.c`.~~ ✅
2. ~~Официално „замразяване" на финалните граници по-горе + roadmap маркиране
   на временните~~ ✅ (LLVM `--rc` изключенията в `docs/language-bg.md` /
   `docs/language-en.md` са премаркирани от „засега" на „отложено за
   post-1.0" / „финална граница"; този документ е roadmap-ът).
3. ~~Release: Unreleased → v1.0.0.~~ ✅ (`v1.0.0`, 2026-08-22 — комит
   `eb15db3`; `BAGA_VERSION` в `include/baga.h` е синхронизиран с `VERSION`,
   след като беше закъснял на 0.9.0 от v0.9.2 насам).
